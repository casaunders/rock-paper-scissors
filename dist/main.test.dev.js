"use strict";

var _require = require("./main.js"),
    generate_com_selection = _require.generate_com_selection;

test("Generates a random outcome.", function () {
  expect(random__number = Math.floor(Math.random() * 3) + 1).toBe(any);
});